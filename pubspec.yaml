name: irregularverbs
description: Application for irregular verbs test

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.3
  path_provider: ^2.0.11
  drift: ^1.7.1
  sqlite3_flutter_libs: ^0.5.8
  path: ^1.8.1

dev_dependencies:
  build_runner: ^2.2.0
  drift_dev: ^1.7.0
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/verbs.sqlite
  uses-material-design: true

